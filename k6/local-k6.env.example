# Source this file before booting the local profile for a reproducible k6 run.
# This file intentionally omits password and secret values.
# Set local-only values in your private shell before booting the app.
# Do not use them for deployed environments.

export DB_USERNAME=root
export MAIL_USERNAME=local@example.invalid
